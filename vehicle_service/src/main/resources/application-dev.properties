# hibernate.use_sql_comments=true

jdbc.url.template=jdbc:postgresql://${DB_HOST}/${DB_NAME}?currentSchema=${DB_SCHEMA}&reWriteBatchedInserts=true

# jakarta.persistence.schema-generation.database.action=update
# jakarta.persistence.jdbc.driver=org.postgresql.Driver
# jakarta.persistence.schema-generation.database.action=update
# jakarta.persistence.jdbc.url=jdbc:postgresql://localhost:5432/client_service_db?currentSchema=emrs_schema&reWriteBatchedInserts=true
# jakarta.persistence.jdbc.user=keoak_user
# jakarta.persistence.jdbc.password=Work@2001


# hibernate.connection.driver_class=org.postgresql.Driver
# hibernate.connection.url=jdbc:postgresql://localhost:5432/client_service_db?currentSchema=emrs_schema&reWriteBatchedInserts=true
# hibernate.connection.username=_user
# hibernate.connection.password=GOGGO



# hibernate.show_sql=true
# hibernate.format_sql=true
# hibernate.use_sql_comments=true
# hibernate.connection.provider_class=org.hibernate.c3p0.internal.C3P0ConnectionProvider
# hibernate.c3p0.min_size=20
# hibernate.c3p0.max_size=50
# hibernate.c3p0.timeout=30
# hibernate.jdbc.batch_size=20
# hibernate.order_inserts=true
# hibernate.order_updates=true
# hibernate.jdbc.batch_versioned_data=true

# hibernate.id.optimizer.pooled.preferred= pooled-lo


# # SQL logging properties
# hibernate.show_sql=true
# hibernate.format_sql=true
# hibernate.use_sql_comments=true


# # Connection pool provider
# hibernate.connection.provider_class=org.hibernate.hikaricp.internal.HikariCPConnectionProvider
# hibernate.dialect=org.hibernate.dialect.PostgreSQLDialectx


# # HikariCP specific properties
# hibernate.hikari.minimumIdle=20
# hibernate.hikari.maximumPoolSize=50
# hibernate.hikari.idleTimeout=30000
# hibernate.hikari.connectionTimeout=20000
# hibernate.hikari.maxLifetime=1200000



# # Batch settings (unchanged)
# hibernate.jdbc.batch_size=20
# hibernate.order_inserts=true
# hibernate.order_updates=true
# hibernate.jdbc.batch_versioned_data=true


# # ID generator settings (unchanged)
# hibernate.id.optimizer.pooled.preferred=pooled-lo




# looging management , io.hahn_software.emrs.aspectj

# logging.level.ma.com.mchange=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.com.trackswiftly.vehicle_service.web=DEBUG
logging.level.com.trackswiftly.vehicle_service.services=DEBUG
# logging.level.io.hahn_software.emrs.aspectj=DEBUG
# logging.level.org.springframework.web=DEBUG


# Multi-tenancy configuration
# hibernate.multiTenancy=DISCRIMINATOR
# hibernate.tenant_identifier_resolver=com.trackswiftly.vehicle_service.utils.CurrentTenantIdentifierResolverImpl


# DataSource configuration
spring.datasource.url=jdbc:postgresql://${DB_HOST}/${DB_NAME}?currentSchema=${DB_SCHEMA}&reWriteBatchedInserts=true
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP configuration
spring.datasource.hikari.minimum-idle=20
spring.datasource.hikari.maximum-pool-size=50
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1200000

# Hibernate configuration
hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
hibernate.format_sql=true
hibernate.use_sql_comments=true
hibernate.jdbc.batch_size=20
hibernate.order_inserts=true
hibernate.order_updates=true
hibernate.jdbc.batch_versioned_data=true
hibernate.id.optimizer.pooled.preferred=pooled-lo

# Multi-tenancy configuration (if needed)
hibernate.multiTenancy=DISCRIMINATOR
hibernate.tenant_identifier_resolver=com.trackswiftly.vehicle_service.utils.CurrentTenantIdentifierResolverImpl